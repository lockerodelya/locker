# Basic redirects for odelya.online
RewriteEngine On

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\.odelya\.online [NC]
RewriteRule ^(.*)$ https://odelya.online/$1 [L,R=301]

# Redirect HTTP to HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://odelya.online/$1 [L,R=301]
